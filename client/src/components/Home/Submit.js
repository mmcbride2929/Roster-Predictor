import { Button, Link } from '@chakra-ui/react'

const Submit = () => {
  return (
    <div>
      <Button>
        <Link to="/results">Submit</Link>
      </Button>
    </div>
  )
}
export default Submit
